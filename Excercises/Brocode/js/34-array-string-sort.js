let fruits = ['Banana', 'Orange', 'Apple', 'Mango'];

// Sort by standard alphabetical order
fruits = fruits.sort();

// Sort by reverse alphabetical order
fruits = fruits.sort().reverse();

for (let fruit of fruits) {
    console.log(fruit);
}